# libfprint-2-tod1-broadcom-canonical
Arch Linux PKGBUILD for the Dell Latitude fingerprint reader (libfprint-2-tod1-broadcom), built from Canonical's Dell OEM pool as a fallback for when the upstream Launchpad git source is down.
